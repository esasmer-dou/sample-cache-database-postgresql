<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.5</version>
        <relativePath/>
    </parent>

    <groupId>com.example.cachedb</groupId>
    <artifactId>sample-cache-database-postgresql</artifactId>
    <version>0.10.1</version>
    <name>sample-cache-database-postgresql</name>
    <description>CacheDB REST API sample using Redis and PostgreSQL</description>

    <properties>
        <java.version>21</java.version>
        <cachedb.groupId>com.reactor.cachedb</cachedb.groupId>
        <cachedb.version>0.10.1</cachedb.version>
        <testcontainers.version>2.0.5</testcontainers.version>
    </properties>

    <repositories>
        <repository>
            <id>cachedb-public</id>
            <name>CacheDB Public Maven Repository</name>
            <url>https://esasmer-dou.github.io/cache-database/maven2</url>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>cachedb-public</id>
            <name>CacheDB Public Maven Repository</name>
            <url>https://esasmer-dou.github.io/cache-database/maven2</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </pluginRepository>
    </pluginRepositories>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>${cachedb.groupId}</groupId>
                <artifactId>cachedb-bom</artifactId>
                <version>${cachedb.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>${cachedb.groupId}</groupId>
            <artifactId>cachedb-spring-boot-starter-postgres</artifactId>
        </dependency>
        <dependency>
            <groupId>${cachedb.groupId}</groupId>
            <artifactId>cachedb-spring-boot-starter-admin</artifactId>
        </dependency>
        <dependency>
            <groupId>${cachedb.groupId}</groupId>
            <artifactId>cachedb-annotations</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>com.vaadin.external.google</groupId>
                    <artifactId>android-json</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-junit-jupiter</artifactId>
            <version>${testcontainers.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>${cachedb.groupId}</groupId>
            <artifactId>cachedb-spring-boot-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-postgresql</artifactId>
            <version>${testcontainers.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>${java.version}</release>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>${cachedb.groupId}</groupId>
                            <artifactId>cachedb-processor</artifactId>
                            <version>${cachedb.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>${cachedb.groupId}</groupId>
                <artifactId>cachedb-maven-plugin</artifactId>
                <version>${cachedb.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>doctor</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <configuredProvider>postgres</configuredProvider>
                    <failOnWarnings>true</failOnWarnings>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>provider-integration</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>integration-test</goal>
                                    <goal>verify</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>production-certification</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>${cachedb.groupId}</groupId>
                        <artifactId>cachedb-maven-plugin</artifactId>
                        <version>${cachedb.version}</version>
                        <executions>
                            <execution>
                                <id>certify-cache-database</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>certify</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
